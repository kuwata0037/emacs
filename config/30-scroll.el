;; --------------------------------------------------
;; smooth-scroll
;; --------------------------------------------------
(use-package smooth-scroll
  :disabled
  :config
  (smooth-scroll-mode)
  )
