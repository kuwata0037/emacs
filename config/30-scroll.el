;; --------------------------------------------------
;; smooth-scroll
;; --------------------------------------------------
(use-package smooth-scroll
  :disabled t
  :config
  (smooth-scroll-mode)
  )
